// event-stream — benign re-export shell (mirror of the real package's structure).
// The actual malicious payload in 2018 lived inside its transitive `flatmap-stream`.
module.exports = require('flatmap-stream');
